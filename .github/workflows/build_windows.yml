name: checking Wrokflow

on:
  pull_request:
    branches: [master, main, develop]

jobs:
  build:
    runs-on: windows-latest
    uses: actions/checkout@v2
    uses: actions/setup-node@v2
    with: 
      node-version: 14
    run: npm install
    run: npm run build
